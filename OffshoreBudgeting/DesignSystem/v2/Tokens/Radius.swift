import SwiftUI

// MARK: - Radius
/// DesignSystemV2 corner radius tokens.
///
/// Keep values 1:1 with legacy `DesignSystem.Radius` when migrating call sites.
enum Radius {
    static let card: CGFloat = 16
}

