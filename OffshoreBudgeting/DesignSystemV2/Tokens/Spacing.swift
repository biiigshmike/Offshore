import SwiftUI

// MARK: - Spacing
/// DesignSystemV2 spacing tokens (Settings scope).
///
/// This file is intentionally small and only includes values that are repeated
/// across Settings and Settings destinations.
enum Spacing {
    static let xxs: CGFloat = 4
    static let xs: CGFloat = 6
    static let s: CGFloat = 8
    static let sPlus: CGFloat = 10
    static let m: CGFloat = 12
    static let l: CGFloat = 16
}
